@model IEnumerable<OMS.Models.Designation>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int Sl = 1;
}

<h3>Designation List</h3>

<p>
    <a asp-action="Create" class="btn btn-success btn-sm"><i class="fa fa-plus" aria-hidden="true"></i> Create</a>
</p>

<table class="table table-bordered table-striped table-hover table-sm">
    <thead>
        <tr style="text-align:center">
            <th>
                SL. No.
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesignationName)
            </th>
            <th>
                Status
            </th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td style="text-align:center">
                @(Sl++)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DesignationName)
            </td>
            <td style="text-align:center">
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
                <partial name="_PartialButtons" model="@item.DesignationId" />
        </tr>
}
    </tbody>
</table>
